class Solution{
//Time Complexity: O(N)- N is the number of integers and nested lists
//Space Complexity: O(N) - N is the number of integers and nested lists
//We take all elements and drop them into a queue to handle them level by level.
//If it’s an integer, we multiply by the current level and add to the result.
//If it’s a list, we just push the inner elements into the queue for the next level.
    public int depthSum(List<NestedInteger> ls)
    {
        int result = 0;
        Queue<NestedInteger> q = new LinkedList<>();
        int level = 1;
        q.addAll(nestedList);
        while(!q.isEmpty())
        {
            int size = q.size();

            for(int i=0;i<size;i++)
            {
                NestedInteger curr = q.poll();
                if(curr.isInteger()){
                    result += curr.getInteger() * level;
                }else{
                    q.addAll(curr.getList());
                }
            }
            level++;
        }
        return result;
    }
}